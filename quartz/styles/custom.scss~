@use "./variables.scss" as *;

// put your custom CSS here!
#quartz-body {
  grid-template-columns: var(--left-sidebar-width, auto) 1fr !important;
  grid-template-areas: "leftsidebar body" !important;
}

.page[data-frame="VALEUR_TROUVÉE"] > #quartz-body {
  grid-template-columns: <largeur-sidebar-gauche> 1fr; /* retire la 3e colonne */
  grid-template-areas: "leftsidebar body";
}

.SELECTEUR_TROUVÉ { display: none !important; }
